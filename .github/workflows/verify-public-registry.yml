name: Verify Public Registry

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  verify-public-registry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Verify public registry manifest
        run: make verify
